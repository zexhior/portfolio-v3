services:
  app:
    build:
      context: .
      target: production
    container_name: portfolio-v3-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      VITE_BASE_URL_N8N: https://training.brice-herizo.cloud/
